import Foundation

enum ModelConfig {
    // Replace with the actual name of your compiled Core ML model resource (without extension)
    static let emotionModelResourceName: String = "CNNEmotions"
}
